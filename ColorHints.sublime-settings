{
    // When to show live hints
    // - always
    // - never
    // - in scope selector, eg 'source.css, source.scss, source.sass, source.less, text.html'
    "live_hints": "always",

    // Interpret hex values with an alpha channel as argb (not rgba)
    "argb_hex": false
}
